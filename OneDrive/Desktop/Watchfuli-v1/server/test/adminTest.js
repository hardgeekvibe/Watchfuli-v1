const chai = require('chai')
const chaiHttp = require('chai-http')
const app = require('../index')
const assert = chai.assert
chai.use(chaiHttp)

suite('Find a manager', () => {
    // test('SHOULD add Admin to database', (done) => {
    //     chai.request(app)
    //         .post
    // })
})